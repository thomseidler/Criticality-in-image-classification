Bootstrap: docker
From: python:3.10

%files
    pyproject.toml
    poetry.lock
    README.md
    src

%post
    pip install poetry
    poetry config virtualenvs.create false
    poetry install --without development